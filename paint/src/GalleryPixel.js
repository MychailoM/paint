import React from "react";

const GalleryPixel = ({ color }) => {
    return (
        <div style={{
            width: 15,
            height: 15,
            backgroundColor: color,
            
        }}></div>
    );
}

export default GalleryPixel;
